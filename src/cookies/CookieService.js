import React from 'react'
import { Cookies } from 'react-cookie'

export const CookieService = (method) => {
    switch (method) {
        case 'get': {
            const get = () => {

            }
        }


        default:
            break;
    }
}
